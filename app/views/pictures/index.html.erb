<h1>投稿一覧</h1>
<<table>
  <tr>
    <th>画像</th>
    <th>メッセージ</th>
  </tr>
<% @picture.each do |pic| %>
  <tr>
    <td><%= pic.image %></td>
    <td><%= pic.content %></td>
  </tr>
<% end %>
</table>
<br>
<% link_to "新規投稿画面に戻る", new_picture_path %>
